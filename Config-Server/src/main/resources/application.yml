server:
  port: 8501
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/hgq0916/cloud-config-center.git
      label: master

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/
